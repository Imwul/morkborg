# Display Translation Integrity — pre-implementation decision

Baseline commit: cdbefec002c047d3f702860bd3b89d04e70bec56. Fresh full suite: 880 passed, 0 failed/skipped. Canonical registry: 1,001 references, 546 tables, 518 rollable, 12,310 rows, 95 creatures, 9 books, 69 procedure references / 58 OracleProcedures. Bundle SHA-256: edb000ad6a23ee13ccdb221732e770977cc6f375aca03621c8f0328441ba60c9.

Fresh full display replay: 347 rendered Korean secondaries, 257 Tier A + 7 Tier B. Remaining 83: one unrelated global exact hit and 82 phrase compositions. Separately, 22 identity outputs already collapse; 632 helper calls return empty. Primary short names and full-title secondary inputs are distinct and remain so.

The 83 item review is private: outputs/display-translation-integrity/tier-c-audit.json. It records each ID, current output, fragments and their actual dictionary/source owners, decision and concrete reason. 65 useful existing helpers remain; 1 misowned exact hit and 17 misleading/malformed compositions are suppressed. This is a review of current output, not blanket exclusion of generated text. In particular region + Discovery, creature name + type, location + noun, and printed encounter number + occupation can be useful without becoming certified names. Curses belongs to AITC festival row 7 rather than the Mythic title. Shape uses the verb 빚어내다, and Wind Strength incorrectly uses noise and the Strength stat.

Implementation: extract the existing owned whole-title evidence unchanged; retain separate search admission and display decisions. Cache owned evidence per registry entry. Use a small reviewed-output admission manifest for the current helper cohort, with fingerprints of source title AND existing output so a changed private pack cannot inherit approval of a different string. It is not a translation dictionary and contains no Korean output. Unreviewed future compositions remain unapproved. Display helper caches invalidate on rules/oracle replacement. UI title sites use a title-specific null-or-span component; body/result Translation stays unchanged. No CSS, primary title, search scoring/index vocabulary, canonical data, IDs, storage, reading or relationship changes.

Expected after: 329 rendered secondary strings = 264 trusted + 65 useful uncertified. Search remains 264 certified and all previous search contracts remain unchanged. No new translation, alias, badge, persistence or navigation.
